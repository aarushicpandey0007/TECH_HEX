import React from 'react';
import Hexagon from '../../view/view1/Hexagon';
import QuizContainer from '../../container/QuizConatiner';

const Quiz: React.FC = () => {
    // const hexagons = Array.from({ length: 49 }); // Total of 49 hexagons for a 7x7 grid

    return (
        <>
            <QuizContainer/>
        </>
    );
};

export default Quiz;